# Optional: only needed if you want API-based generation instead of the default local extractive generator
OPENAI_API_KEY=

# Optional: use this only if you want an OpenAI-compatible endpoint
OPENAI_BASE_URL=
